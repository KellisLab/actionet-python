# Preprocessing

Import, filtering, subsetting, and normalization utilities that operate on
`AnnData` objects (including backed / HDF5-streamed inputs).

::: actionet.preprocessing
    options:
      members:
        - import_anndata_generic
        - filter_anndata
        - compute_filter_masks
        - apply_filter
        - subset_anndata
        - normalize_anndata
        - decompress_backed_storage
