# Backed persistence

Helpers for working with backed (HDF5-streamed) `AnnData` objects — checkpoint,
materialize, subset in place, and control auto-persist behavior.

::: actionet._backed_persist
    options:
      show_root_heading: false
      members:
        - checkpoint_backed
        - get_auto_persist
        - materialize_backed
        - set_auto_persist
        - subset_backed_inplace
